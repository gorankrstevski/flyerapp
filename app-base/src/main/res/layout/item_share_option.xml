<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="@dimen/share_dialog_item_size"
    android:layout_height="@dimen/share_dialog_item_size"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    tools:ignore="UseCompoundDrawables">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="@dimen/share_dialog_item_image_size"
        android:layout_height="@dimen/share_dialog_item_image_size" />

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="16dp"
        android:gravity="center"
        android:textColor="@color/text_color_dark" />

</LinearLayout>